<form class="{{cssClass}}" autocomplete="off">
  <header class="sheet-header">
    <img class="profile-img" src="{{item.img}}" data-edit="img" title="{{item.name}}"/>
    <div class="header-fields">
      <h1 class="charname"><input name="name" type="text" value="{{item.name}}" placeholder="Name"/></h1>
    </div>
   <div>
     <table>
       <tr>
         <th>{{localize "faserip.weapon.range"}}</th>
         <th>{{localize "faserip.weapon.price"}}</th>
         <th>{{localize "faserip.weapon.damage"}}</th>
         <th>{{localize "faserip.weapon.type"}}</th>
         <th>{{localize "faserip.weapon.rate"}}</th>
         <th>{{localize "faserip.weapon.shots"}}</th>
         <th>{{localize "faserip.weapon.material"}}</th>
       </tr>
       <td>
         <input name="data.range" type="text" value="{{data.range}}" data-dtype="Number" />
       </td>
       <td>
        <input name="data.price" type="text" value="{{data.price}}" data-dtype="Number" />
      </td>
      <td>
        <input name="data.damage" type="text" value="{{data.damage}}" data-dtype="Number" />
      </td>
      <td>
        <select name="data.attackTypes">
          {{#select data.attackTypes}}
          {{#each config.attackTypes as |name type|}}
          <option value="{{type}}">{{name}}</option>
          {{/each}}
          {{/select}}
        </select>
      </td>
      <td>
        <input name="data.rate" type="text" value="{{data.rate}}" data-dtype="Number" />
      </td>
      <td>
        <input name="data.shots" type="text" value="{{data.shots}}" data-dtype="Number" />
      </td>
      <td>
        <input name="data.material" type="text" value="{{data.material}}" data-dtype="Number" />
      </td>
     </table>
     {{editor content=data.description target="data.description" button=true owner=owner editable=editable}}
   </div>
</form>
